var todo;
todo = { age: 41 };
function totalLength1(x, y) {
    var total = x.length + y.length;
    return total;
}
function totalLength2(x, y) {
    var total = x.length + y.length;
    return total;
}
